package Leetcode;

public class Num394 {
    String ans;
    public String decodeString(String s) {
        int len = s.length();
        ans = helper(s, len);
    return ans;
    }

    int i =0;
    public String helper(String s, int len){
        if(len == i)
            return;
        if(s.charAt(i) == )

    }
}
